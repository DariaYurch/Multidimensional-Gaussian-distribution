# Multidimensional-Gaussian-distribution

В данном репозитории представлен Jupyter Notebook: multidimensional_Gaussian_distribution.ipynb


Работа посвящена восстановлению пропущенных значений на основе предположения о совместном многомерном нормальном распределении признаков.

Основная идея: если признаки распределены совместно нормально, то для частично наблюдаемого объекта можно вычислить условное математическое ожидание пропущенных компонент по наблюдаемым и использовать его как оптимальную (в смысле MSE) оценку.

---

## 📌 Цель работы

В ноутбуке последовательно решаются следующие задачи:

1. Смоделировать механизм пропусков MCAR (Missing Completely At Random).
2. Оценить параметры многомерного нормального распределения:
   - вектор средних `m`
   - ковариационную матрицу `S`
3. Реализовать восстановление пропусков:
   - одного признака
   - нескольких признаков
   - целевой переменной
4. Сравнить качество восстановления с базовыми методами (например, замена средним).
5. Проанализировать поведение метода в разных сценариях.

---

## 🧠 Математическая модель

Пусть случайный вектор разбит на наблюдаемую и пропущенную части:

- x = [x_obs, x_miss]
- mu = [mu_obs, mu_miss]
- ковариационная матрица имеет блочную структуру:  
Sigma =  
Sigma_oo Sigma_om  
Sigma_mo Sigma_mm


Для многомерного нормального распределения выполняется:  
E[x_miss | x_obs] = mu_miss + Sigma_mo * Sigma_oo ^ -1 (x_obs - mu_obs)  
Именно это условное математическое ожидание используется в ноутбуке для импутации.

Чтобы обеспечить численную устойчивость при обращении матрицы, применяется регуляризация.

---

## 🔬 Что реализовано в ноутбуке

В работе реализованы:

- генерация MCAR-пропусков с фиксированным `seed`;
- оценка параметров гауссовской модели по обучающей выборке;
- импутация:
  - одиночных пропусков;
  - множественных пропусков;
  - пропусков в целевой переменной;
- сравнение с простой заменой средним;
- оценка качества восстановления (например, MAE);
- анализ различий между методами.
---
## 🏆Итоговые выводы
1. Гауссовская импутация эффективна при наличии корреляций  
Если признаки действительно коррелированы и близки к совместному нормальному распределению, использование условного математического ожидания позволяет получать более точное восстановление по сравнению с заменой средним. Метод учитывает всю ковариационную структуру данных.  
2. При пропусках в таргете метод сводится к линейной регрессии  
В случае пропусков в целевой переменной формула условного ожидания:
E[y | X] = mu_y + sigma_yX * sigma_XX ^ -1 (X - mu_X)
фактически совпадает с линейной регрессией. На практике результаты оказываются практически идентичными (с учётом численной регуляризации).  
3. Метод чувствителен к предположениям  
Эффективность зависит от:  
- близости данных к многомерному нормальному распределению;  
- механизма пропусков MCAR;  
- устойчивости оценки ковариационной матрицы.  
При нарушении этих предположений (выбросы, сильная негауссовость, MAR/MNAR) качество восстановления может снижаться.
---
## Общий итог:  
Гауссовская импутация — это строгий вероятностный метод, который:  
- учитывает взаимосвязи между признаками;  
- имеет чёткое статистическое обоснование;  
- естественным образом переходит в линейную регрессию при пропусках в целевой переменной;  
- требует аккуратной численной реализации и соблюдения предположений модели.

